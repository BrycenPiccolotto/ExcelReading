If you are using this program in Netbeans, import these jar files into a library to allow for the code to run

You can do this by right clicking on the "Libraries" folder under the project, adding a new library, placing the .jar files into that library and then adding that library to the project